#include "random_lib.h"
#include <stdlib.h>
#include <string.h>

// prototipos de funciones
char *dectobin(int);
void strrev(char *);
int andmask(int, int);
int ormask(int, int);
int xormask(int, int);

